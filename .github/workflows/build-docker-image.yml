name: Build Docker Image

on:
  workflow_dispatch:
    inputs:
      release:
        type: string
        required: true
        description: 'Internal release version counter'
        default: '0'
      grafana:
        type: string
        required: true
        description: 'Grafana release version'
        default: '12.4.0'

jobs:
  push_to_registry:
    name: Push image to Github container registry
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      attestations: write
      id-token: write
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Artifactory Container Registry
        uses: docker/login-action@v3
        with:
          registry: artifactory.mitel.com
          username: ${{ secrets.ARTIFACTORY_USER }}
          password: ${{ secrets.ARTIFACTORY_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ vars.ARTIFACTORY_IMG_URL_PUBLIC }}/grafana
            ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=${{ github.event.inputs.grafana }}-r${{ github.event.inputs.release }}-tumbleweed
      
      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          build-args: |
            GRAFANA_VERSION=${{ github.event.inputs.grafana }}
            RELEASE_VERSION=${{ github.event.inputs.release }}
            MEND_EMAIL=${{ secrets.MEND_EMAIL }}
            MEND_URL=${{ secrets.MEND_URL }}
            MEND_USER_KEY=${{ secrets.MEND_USER_KEY }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Scan Docker Image
        if: ${{ success() }}
        env:
          IMAGE: ${{ vars.ARTIFACTORY_IMG_URL_PUBLIC }}/grafana:${{ github.event.inputs.grafana }}-r${{ github.event.inputs.release }}-tumbleweed
          GRAFANA_VERSION: ${{ github.event.inputs.grafana }}
          RELEASE_VERSION: ${{ github.event.inputs.release }}
          MEND_EMAIL: ${{ secrets.MEND_EMAIL }}
          MEND_URL: ${{ secrets.MEND_URL }}
          MEND_USER_KEY: ${{ secrets.MEND_USER_KEY }}
        run: |
          rm -f ${{ github.workspace }}/mend
          curl https://downloads.mend.io/cli/linux_amd64/mend -o ${{ github.workspace }}/mend && chmod +x ${{ github.workspace }}/mend
          ${{ github.workspace }}/mend image ${IMAGE,,} -s "OpenScape UC Portfolio//OSEM third party applications//grafana-osem-docker-image - $GRAFANA_VERSION-r$RELEASE_VERSION-tumbleweed"

      - name: Create a release
        if: ${{ success() }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GRAFANA_VERSION: ${{ github.event.inputs.grafana }}
          RELEASE_VERSION: ${{ github.event.inputs.release }}
          IMAGE: ${{ vars.ARTIFACTORY_IMG_URL_PUBLIC }}/grafana:${{ github.event.inputs.grafana }}-r${{ github.event.inputs.release }}-tumbleweed
        run: |
          # Add step summary
          {
            echo "# Grafana"
            echo ""
            echo "Running in an openSUSE Tumbleweed Image."
            echo ""
            echo "${IMAGE}"
            echo ""
            echo "### Included versions"
            echo ""
            echo "| Package Name              | Version         |"
            echo "|---------------------------|-----------------|"
            echo "| Grafana | $GRAFANA_VERSION |"
          } >> $GITHUB_STEP_SUMMARY
          
          cat > ${{ github.workspace }}/release-notes.md <<EOF
          ## Grafana release ${{ github.event.inputs.grafana }}-${{ github.event.inputs.release }}
          - **Grafana version**: $GRAFANA_VERSION
          ```
          docker pull ${IMAGE}
          ```

          EOF
          gh release create "$GRAFANA_VERSION-r$RELEASE_VERSION-tumbleweed" \
              --repo="$GITHUB_REPOSITORY" \
              --title="$GRAFANA_VERSION-r$RELEASE_VERSION-tumbleweed" \
              --notes-file release-notes.md
